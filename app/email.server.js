import nodemailer from "nodemailer";
import { formatGBP } from "./pricing.js";
export function renderCustomerEmail({quote,revision,paymentUrl}){return {subject:`Your Stow & Settle quotation ${quote.reference}`,text:`Hello ${quote.customerName},\n\nYour bespoke seating quotation is ready.\n\nGrand total: ${formatGBP(revision.grandTotalPence)} (GBP, inclusive of VAT)\n\nAccept quote and pay: ${paymentUrl}\n\nKind regards,\nStow & Settle`,html:`<p>Hello ${escapeHtml(quote.customerName)},</p><p>Your bespoke seating quotation <strong>${escapeHtml(quote.reference)}</strong> is ready.</p><p><strong>Grand total: ${formatGBP(revision.grandTotalPence)}</strong> (GBP, inclusive of VAT)</p><p><a href="${escapeHtml(paymentUrl)}">Accept quote &amp; pay</a></p><p>Kind regards,<br>Stow &amp; Settle</p>`}}
export async function sendCustomerEmail(message){if(process.env.EMAIL_PROVIDER==="preview"||!process.env.EMAIL_PROVIDER){return {messageId:`preview-${Date.now()}`,preview:true}}if(process.env.EMAIL_PROVIDER!=="smtp")throw new Error("Unsupported EMAIL_PROVIDER");const transport=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:Number(process.env.SMTP_PORT)===465,auth:{user:process.env.SMTP_USER,pass:process.env.SMTP_PASSWORD}});return transport.sendMail({from:process.env.EMAIL_FROM,...message})}
function escapeHtml(v){return String(v).replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));}
